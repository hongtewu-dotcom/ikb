---
type: release
component: agent-teams
version: 2.0.0
date: 2026-08-26
status: installed-controlled-canary-passed
---

# Agent Teams 2.0.0 发布记录

## 发布对象

| 项目 | 值 |
|---|---|
| 源码 commit | `274fede` |
| marketplace | `ikb-agent-team` |
| plugin id | `agent-teams@ikb-agent-team` |
| version | `2.0.0` |
| Codex CLI | `0.149.1` |
| 候选包 tree hash | `b3b5e673f22a752526dca07a998575351aa03c2f7ebabb453424987292c8d8e9` |
| 候选构建 | `/Users/htwu/projects/_personal/ikb/projects/agent-team/dist/codex-marketplace` |
| 只读发布快照 | `/Users/htwu/.local/share/codex-marketplaces/ikb-agent-team/2.0.0-b3b5e673` |
| 安装缓存 | `/Users/htwu/.codex/plugins/cache/ikb-agent-team/agent-teams/2.0.0` |

tree hash 的计算口径是：按相对路径排序，对 marketplace 六个文件逐个做 SHA-256，再对“文件 hash + 相对路径”清单做一次 SHA-256。

## 本地与安装证据

- `make test`：23 passed。
- `make validate-codex`：6 written，0 warning，0 error；结构校验无问题。
- Codex 插件本地校验器：passed。
- 隔离 `CODEX_HOME` 中，marketplace 注册和 2.0.0 安装成功。
- 默认 marketplace 指向带版本和 hash 的只读发布快照，不依赖活跃源码或可重建 `dist/`。
- 默认安装后，`codex plugin list --json` 显示 installed、enabled、version 2.0.0。
- 候选构建、只读发布快照与安装缓存内容一致。
- 缓存只有 manifest、LICENSE 和三个 Skill；没有 Agent、command、Hook、MCP 或全局软链接。

## 新任务 canary

thread：`01a03d4d-07bb-7451-b798-5cd6b1675e07`

canary 在一个没有 `.agents/skills` 的全新临时仓运行。任务启动时实际发现：

```text
agent-teams:agent-team-delegate
agent-teams:agent-team-feature
agent-teams:agent-team-review
```

随后读取单文件 `note.md` 并返回 `release_channel=canary`。rollout 中 `spawn_agent=0`、`wait_agent=0`、全部 collaboration call 为 0，证明插件已加载且没有把单文件任务团队化。

当前 Codex CLI 的 `--ephemeral` 线程不能承载原生 spawn，因此多 Agent canary 必须使用全新持久任务。被排除的失败样本记录在 [行为 A/B](../evals/2026-08-26-codex-ab.md)。

## 回滚

```bash
codex plugin remove agent-teams@ikb-agent-team --json
codex plugin marketplace remove ikb-agent-team --json
```

回滚只移除默认安装和 marketplace，不删除 IKB 源码、发布记录或行为证据。
