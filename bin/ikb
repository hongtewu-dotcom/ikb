#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export IKB_PROJECT_ROOT="${IKB_PROJECT_ROOT:-$ROOT}"
exec node --no-warnings=ExperimentalWarning --experimental-strip-types "$ROOT/src/cli.ts" "$@"
